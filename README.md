# DynamicArchiver

`DynamicArchiver` is an Objective-C runtime-assisted archiving library for inspecting, boxing, encoding, and 
reconstructing arbitrary Objective-C objects using Foundation serialization mechanisms such as `NSKeyedArchiver`.